小米一面

问项目

一些简单八股

块级作用域 函数作用域 全局作用域

es6 特性

讲讲类组件

还有一些...忘了 反正都是常见的八股

编程题：（忘了一道）

1. 分别输出下面的结果

```js
for(var i = 0 ; i < 5 ; i++)
{
	setTimeout(() => {
		console.log(i)
	},100)
}

for(let i = 0 ; i < 5 ; i++)
{
	setTimeout(() => {
	console.log(i)
	},100)
}

for(var i = 0 ; i < 5 ; i++)
{
	function printText(text)
	{
		setTimeout(() => {
		console.log(text)
		},100)
	}
	printText(i);
}

for(var i = 0 ; i < 5 ; i++)
{
		setTimeout(() => {
		var temp = i;
		console.log(temp);
		},100)
}

```

2.

点击一次后控制台的 输出顺序 以及 输出结果

```js
function App()
{
	const [count,setCount] = useState(0);
	console.log(count);
	
	return (
		<div>
			{count}
			<button onClick = {
			() => {
				setCount(count + 1)
				setTimeout(()=>{
					console.log(count)
				},1000)
			}
			}>
			+1
			</button>
		</div>
	)
}

```

3.

```js
全排列
```

4.

```js
给有序数组arr = [-4,-1,0,3,5]，返回平方后的排序

答 return arr.map(item => item * item).sort((a,b) => a-b);

问 时间复杂度？ 给出优化后的写法

答：负数正数分开 ＋ 双指针
```

